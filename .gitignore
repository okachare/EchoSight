# Raw scan data — large binary files, do not commit
*.tiff
*.tif
*.raw

# Converted images — regeneratable from script, keep repo light
*.jpg
*.jpeg
*.png
*.bmp

# Geti project data and model weights — large binaries
data/
models/
*.xml
*.bin
*.onnx
*.pt
*.pth

# Geti logs and run artifacts — drop into workspace for analysis but don't commit
logs/
runs/
*.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-*/
venv/
dist/
build/
*.spec
Deployment/EchoSight_Inference_GUI/portable/
Deployment/EchoSight_Inference_GUI/build/
Deployment/EchoSight_Inference_GUI/dist/

# Environment
.env
*.env

# OS
.DS_Store
Thumbs.db
desktop.ini

# Local GeTi training runs contain generated datasets, databases, checkpoints,
# exports, media, and telemetry. Keep the evaluation evidence outside this run
# directory under version control instead.
Debug/NVL_Geti_Run/
